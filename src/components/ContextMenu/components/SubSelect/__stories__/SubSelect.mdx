import { Title, Meta, Canvas, Controls } from '@storybook/blocks';
import { CodeBlock } from '@storybook-utils/components';
import * as SubSelectStories from './SubSelect.stories';
import { i18n } from '@i18n';
import ReactMarkdown from 'react-markdown';

<Meta of={SubSelectStories} />

<Title>ContextMenu.SubSelect</Title>

<Canvas of={SubSelectStories.Default} />

<ReactMarkdown>{'## ' + i18n.t('Usage')}</ReactMarkdown>

<CodeBlock of={SubSelectStories.Default} />

<ReactMarkdown>{'## ' + i18n.t('Props')}</ReactMarkdown>

<Controls />
