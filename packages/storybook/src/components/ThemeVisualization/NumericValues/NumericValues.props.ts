import { type NumericItemType } from '@sb/components/ThemeVisualization/types/Items';

export type Props = {
  /**
   * List of numeric theme design tokens.
   */
  numericValues: NumericItemType[];
};
