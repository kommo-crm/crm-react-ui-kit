import { type RadioThemeType } from './Radio.themes';

export type RadioProps = {
  /**
   * `class` attribute for `Radio`.
   */
  className?: string;
  /**
   * Object with CSS theme properties.
   */
  theme: RadioThemeType;
};
