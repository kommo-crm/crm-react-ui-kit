import { Title, Meta, Canvas, Controls } from '@storybook/blocks';
import { CodeBlock } from '@storybook-utils/components';
import * as CounterBadgeStories from './CounterBadge.stories';
import { i18n } from '@i18n';
import ReactMarkdown from 'react-markdown';

<Meta of={CounterBadgeStories} />

<Title>CounterBadge</Title>

<Canvas of={CounterBadgeStories.Default} />

<ReactMarkdown>{'## ' + i18n.t('Usage')}</ReactMarkdown>

<CodeBlock of={CounterBadgeStories.Default} />

<ReactMarkdown>{'## ' + i18n.t('Props')}</ReactMarkdown>

<Controls />
