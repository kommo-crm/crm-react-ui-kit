import { Title, Meta, Canvas } from '@storybook/blocks';
import * as ListStories from './List.stories';
import { TextPrimaryTheme } from '@ui-kit/components/Text';
import { ThemeVisualization } from '@storybook-utils/components';
import { i18n } from '@i18n';
import ReactMarkdown from 'react-markdown';

<Meta title="Typography/List/Themes" of={ListStories} />

<Title>{i18n.t('Themes')}</Title>

<ReactMarkdown>{'## ' + i18n.t('Theming with Text component')}</ReactMarkdown>

<ReactMarkdown>{i18n.t('List theming description')}</ReactMarkdown>

```jsx
import { Text } from '@kommo-crm/crm-react-ui-kit/Text';
import { List } from '@kommo-crm/crm-react-ui-kit/List';
import { TextPrimaryTheme } from '@kommo-crm/crm-react-ui-kit/Text';

function App() {
  return (
    <Text as="div" size="l" theme={TextPrimaryTheme}>
      <List type="bulleted">
        <List.Item>{i18n.t('Bulleted List level')} 1</List.Item>
        <List.Item>{i18n.t('Bulleted List level')} 1</List.Item>
      </List>
    </Text>
  );
}
```

<Canvas of={ListStories.Default} />

## TextPrimaryTheme

```jsx
import { TextPrimaryTheme } from '@kommo-crm/crm-react-ui-kit/Text';
```

<ThemeVisualization theme={TextPrimaryTheme} of={ListStories.Default} />
